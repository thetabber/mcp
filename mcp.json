{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "thetabber": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@thetabber/mcp"],
      "env": {
        "TABBER_API_KEY": "${TABBER_API_KEY}"
      }
    }
  }
}
